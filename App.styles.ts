import {StyleSheet} from 'react-native';
import {colorPrimary} from './src/theme/Colors';

export const styles = StyleSheet.create({
    rootContainer: {
        flex: 1,
        backgroundColor: colorPrimary,
    },
});
